[build]
  publish = "studio"   # Full Crate Studio with vintage aesthetic
  command = "echo 'No build needed for static HTML'"

[[headers]]
  for = "/*"
  [headers.values]
    Cache-Control = "no-cache, must-revalidate"

[[redirects]]
  from = "/grabber/*"
  to = "/grabber/:splat"
  status = 200